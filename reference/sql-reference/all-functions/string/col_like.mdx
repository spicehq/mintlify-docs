---
title: "COL_LIKE"
---

1. [📚REFERENCE](/reference)
2. [SQL Reference](/reference/sql-reference)
3. [SQL Functions](/reference/sql-reference/all-functions)
4. [String](/reference/sql-reference/all-functions/string)

Tests whether an expression column matches a pattern column. Comparisons are case-sensitive.

### Syntax

#### COL\_LIKE(*expression\_col* varchar, *pattern\_col* varchar) → boolean

* expression\_col: A column containing an expression to compare.

* pattern\_col: A column containing the pattern to compare to the expression.

**Examples**

<CodeGroup>
  ```bash COL_LIKE example
  CREATE TABLE $scratch.names (name varchar, pat varchar)
  AS VALUES ('john', '%oh%'), ('jacob', '%aco%'), ('bill', '%ob%');

  SELECT name 
  FROM $scratch.names 
  WHERE col_like (name, pat);

  -- john
  -- jacob
  ```
</CodeGroup>
