---
title: "DATE_TRUNC"
---

1. [📚REFERENCE](/reference)
2. [SQL Reference](/reference/sql-reference)
3. [SQL Functions](/reference/sql-reference/all-functions)
4. [Date/Time](/reference/sql-reference/all-functions/date-time)

Truncates the date or timestamp to the indicated precision.

### Syntax

#### DATE\_TRUNC(*time\_unit* LITERAL, *date\_timestamp\_expression* DATE OR TIMESTAMP) → DATE

* time\_unit: The time unit that the date or timestamp needs to be truncated at. This must be the literal value of ‘YEAR’, ‘MONTH’, ‘DAY’, ‘HOUR’, ‘MINUTE’, or ‘SECOND’.

* date\_timestamp\_expression: The date or timestamp to truncate. This value must be a literal in the date or timestamp format.

**Examples**

<CodeGroup>
  ```bash DATE_TRUNC example
  SELECT DATE_TRUNC('MONTH', '2021-12-24')
  -- 2021-12-01
  ```
</CodeGroup>

<CodeGroup>
  ```bash DATE_TRUNC example
  SELECT DATE_TRUNC('MINUTE', CAST('2021-12-24 12:28:33' as TIMESTAMP))
  -- 2021-12-24 12:28:00
  ```
</CodeGroup>

<CodeGroup>
  ```bash DATE_TRUNC example
  SELECT DATE_TRUNC('HOUR', '2021-12-24 12:28:33')
  -- 2021-12-24
  ```
</CodeGroup>
