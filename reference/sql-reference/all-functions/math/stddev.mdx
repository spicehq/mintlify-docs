---
title: "STDDEV"
---

1. [📚REFERENCE](/reference)
2. [SQL Reference](/reference/sql-reference)
3. [SQL Functions](/reference/sql-reference/all-functions)
4. [Math](/reference/sql-reference/all-functions/math)

Returns the standard deviation of non-NULL values in a column with a numeric data type. If all records inside a group are NULL, returns NULL.

### Syntax

#### STDDEV(*numeric\_expression* double) → double

* numeric\_expression: The set of records to calculate the standard deviation for.

**Examples**

<CodeGroup>
  ```bash STDDEV example
  SELECT STDDEV(gas_used) 
  FROM eth.recent_blocks
  -- 2.2596650338461766
  ```
</CodeGroup>

#### STDDEV(*numeric\_expression* float) → double

* numeric\_expression: The set of records to calculate the standard deviation for.

**Examples**

<CodeGroup>
  ```bash STDDEV example
  SELECT STDDEV(gas_used) 
  FROM eth.recent_blocks
  -- 2.2596650338461766
  ```
</CodeGroup>

#### STDDEV(*numeric\_expression* int32) → double

* numeric\_expression: The set of records to calculate the standard deviation for.

**Examples**

<CodeGroup>
  ```bash STDDEV example
  SELECT STDDEV(transaction_count) 
  FROM eth.recent_blocks
  -- 1.3670214641762426
  ```
</CodeGroup>

#### STDDEV(*numeric\_expression* int64) → double

* numeric\_expression: The set of records to calculate the standard deviation for.

**Examples**

<CodeGroup>
  ```bash STDDEV example
  SELECT STDDEV(transaction_count) 
  FROM eth.recent_blocks
  -- 1.3670214641762426
  ```
</CodeGroup>
