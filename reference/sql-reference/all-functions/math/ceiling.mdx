---
title: "CEILING"
---

1. [📚REFERENCE](/reference)
2. [SQL Reference](/reference/sql-reference)
3. [SQL Functions](/reference/sql-reference/all-functions)
4. [Math](/reference/sql-reference/all-functions/math)

Returns the nearest equal or larger value of the input expression. Can also be called using CEIL().

### Syntax

#### CEILING(*numeric\_expression* NUMERIC) → INTEGER

* numeric\_expression: The number (`DOUBLE`, `FLOAT`, `INTEGER`) to compute the ceiling.

**Examples**

<CodeGroup>
  ```bash CEILING example
  SELECT CEILING(3.1459)
  -- 4
  ```
</CodeGroup>

<CodeGroup>
  ```bash CEILING example
  SELECT CEIL(37.775420706711)
  -- 38
  ```
</CodeGroup>

<CodeGroup>
  ```bash CEILING example
  SELECT CEIL(-37.775420706711)
  -- -37
  ```
</CodeGroup>

<CodeGroup>
  ```bash CEILING example
  SELECT CEIL(0)
  -- 0
  ```
</CodeGroup>
