---
title: "SIGN"
---

1. [📚REFERENCE](/reference)
2. [SQL Reference](/reference/sql-reference)
3. [SQL Functions](/reference/sql-reference/all-functions)
4. [Math](/reference/sql-reference/all-functions/math)

Returns the sign of the input expression.

### Syntax

#### SIGN(*numeric\_expression* double) → int

* numeric\_expression: Input expression.

**Examples**

<CodeGroup>
  ```bash SIGN example
  SELECT SIGN(10.3)
  -- 1
  ```
</CodeGroup>

#### SIGN(*numeric\_expression* int32) → int32

* numeric\_expression: Input expression.

**Examples**

<CodeGroup>
  ```bash SIGN example
  SELECT SIGN(-5)
  -- -1
  ```
</CodeGroup>

#### SIGN(*numeric\_expression* int64) → int64

* numeric\_expression: Input expression.

**Examples**

<CodeGroup>
  ```bash SIGN example
  SELECT SIGN(24)
  -- 1
  ```
</CodeGroup>

#### SIGN(*numeric\_expression* float) → int

* numeric\_expression: Input expression.

**Examples**

<CodeGroup>
  ```bash SIGN example
  SELECT SIGN(0.0)
  -- 0
  ```
</CodeGroup>

### Usage Notes

1 is returned if the input expression is positive. -1 is returned if the input expression is negative. 0 is returned if the input expression is 0.
