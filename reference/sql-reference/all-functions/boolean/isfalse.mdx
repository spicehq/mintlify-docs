---
title: "ISFALSE"
---

1. [📚REFERENCE](/reference)
2. [SQL Reference](/reference/sql-reference)
3. [SQL Functions](/reference/sql-reference/all-functions)
4. [Boolean](/reference/sql-reference/all-functions/boolean)

Tests whether the input expression is false. If it is, returns a value of true.

### Syntax

#### ISFALSE(*boolean\_expression* boolean) → boolean

* boolean\_expression: The input expression, which must be of type `BOOLEAN`.

**Examples**

<CodeGroup>
  ```bash Create a table containing one column of boolean data type
  CREATE TABLE $scratch.test_table 
    (
      test_column BOOLEAN
    )

  -- true, Table created
  ```
</CodeGroup>

<CodeGroup>
  ```bash Create two rows containing boolean values
  INSERT INTO $scratch.test_table 
  VALUES  (true), 
          (false)

  -- Fragment, Records, Path, Metadata, Partition, FileSize, IcebergMetadata, fileschema, PartitionData
  -- 0_0, 1, s3://<s3-bucket-path>, , 0, <file-size>, <iceberg-metadata>, <file-schema>, null
  ```
</CodeGroup>

<CodeGroup>
  ```bash Run the ISFALSE query
  SELECT ISFALSE(test_column)
  FROM $scratch.test_table

  -- false 
  -- true 
  ```
</CodeGroup>
